@namespace("com.github.devraghav.domain.project")
protocol NewProject {
	record NewProject {
		string name;
		string description;
		string status;
		string authorId;
		union {null,map<string>} tags;
	}
}